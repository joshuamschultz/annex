.nested-fields.form-group
  .wrapper 
    .row
      .col
        = f.association :part, label_method: :part_number
      .col 
        = f.input :quantity
      .col 
        = f.association :imports_part, :label_method => lambda { |import_part| "#{import_part.part.part_number}" "-" "#{import_part.import.assigned_number}" }, label: 'Import Number'
    .row 
      = link_to_remove_association "remove part", f

        