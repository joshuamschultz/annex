<%= form_with(model: export_type, local: true) do |form| %>
  <% if export_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(export_type.errors.count, "error") %> prohibited this export_type from being saved:</h2>

      <ul>
        <% export_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type_code %>
    <%= form.text_field :type_code %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
